<Page
    x:Class="VoiceInput.Pages.LyricsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="12" Spacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="歌词识别" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,4" />
        <TextBlock Grid.Row="1" Text="上传音频文件 自动识别并生成 LRC 格式歌词" FontSize="13" Foreground="#999999" Margin="0,0,0,16" />

        <Grid Grid.Row="2" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1E1E1E" CornerRadius="8" BorderBrush="#555555" BorderThickness="2" x:Name="UploadBorder" AllowDrop="True" DragOver="OnDragOver" DragLeave="OnDragLeave" Drop="OnDrop" PointerReleased="OnUploadClick" Height="400" VerticalAlignment="Top">
                <Grid Padding="24" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <StackPanel Spacing="16" HorizontalAlignment="Center" x:Name="EmptyUploadState">
                        <FontIcon Glyph="&#xE118;" FontSize="48" Foreground="#555555" HorizontalAlignment="Center" />
                        <TextBlock Text="点击上传文件" FontSize="14" Foreground="#FFFFFF" HorizontalAlignment="Center" />
                        <TextBlock Text="支持 WAV、MP3 格式 最大 10MB" FontSize="12" Foreground="#777777" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" />
                    </StackPanel>
                    
                    <StackPanel Spacing="16" HorizontalAlignment="Center" x:Name="FileSelectedState" Visibility="Collapsed">
                        <FontIcon Glyph="&#xE189;" FontSize="48" Foreground="#0078D4" HorizontalAlignment="Center" />
                        <TextBlock x:Name="FileNameTxt" Text="audio_file.wav" FontSize="14" Foreground="#FFFFFF" FontWeight="Bold" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" />
                        <TextBlock x:Name="FileSizeTxt" Text="4.2 MB" FontSize="12" Foreground="#999999" HorizontalAlignment="Center" />
                        <Button Content="删除文件" Click="OnDeleteFileClick" HorizontalAlignment="Center" Background="#D83B01" Foreground="#FFFFFF" BorderThickness="0" Padding="12,6" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="1" RowSpacing="12">
                <Border Background="#1E1E1E" CornerRadius="8" Padding="16" Height="400">
                    <Grid>
                        <TextBlock x:Name="IdleStateTxt" Text="识别出的歌词将在这里显示" FontSize="14" Foreground="#777777" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        
                        <StackPanel x:Name="LoadingState" Spacing="12" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                            <ProgressRing IsActive="True" Width="48" Height="48" Foreground="#0078D4" />
                            <TextBlock Text="正在识别中..." FontSize="14" Foreground="#FFFFFF" HorizontalAlignment="Center" />
                            <TextBlock x:Name="TimerTxt" Text="已耗时: 0s" FontSize="12" Foreground="#999999" HorizontalAlignment="Center" />
                        </StackPanel>

                        <TextBox x:Name="LyricsTxt" FontFamily="Consolas" FontSize="13" Foreground="#FFFFFF" Background="Transparent" BorderThickness="0" AcceptsReturn="True" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Auto" Visibility="Collapsed" IsReadOnly="False" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
                <Button x:Name="StartBtn" Content="开始识别" Click="OnStartRecognizeClick" Background="#0078D4" Foreground="#FFFFFF" BorderThickness="0" Padding="20,8" IsEnabled="False" />
                <Button x:Name="ExportBtn" Content="导出 LRC" Click="OnExportClick" Background="Transparent" BorderBrush="#555555" BorderThickness="1" Foreground="#FFFFFF" Padding="20,8" IsEnabled="False" />
                <Button x:Name="CopyBtn" Content="复制到剪贴板" Click="OnCopyClick" Background="Transparent" BorderBrush="#555555" BorderThickness="1" Foreground="#FFFFFF" Padding="20,8" IsEnabled="False" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
